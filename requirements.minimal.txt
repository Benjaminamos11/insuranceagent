# Minimal requirements for Swiss Insurance Agent deployment
# Core AI and API packages
openai>=1.35.0
anthropic>=0.25.0
groq>=0.9.0

# Web framework
quart>=0.19.0
quart-cors>=0.7.0
werkzeug>=3.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Authentication and payments
pyjwt>=2.8.0
bcrypt>=4.0.0
stripe>=10.0.0

# Environment and configuration
python-dotenv>=1.0.0

# Async HTTP client
aiohttp>=3.9.0

# Optional: Email support (only if using email features)
# imaplib is built-in
# smtplib is built-in

# Utility packages
requests>=2.31.0
python-dateutil>=2.8.0

# JSON handling (built-in)
# asyncio (built-in)
# email (built-in)
# datetime (built-in)
# typing (built-in) 